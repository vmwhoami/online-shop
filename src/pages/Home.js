import React from 'react';
import Layout from '../layout';

const Home = () => (
  <Layout>
    <h2>This is going to be the home page</h2>
  </Layout>
);

// Home.propTypes = {

// };

export default Home;
